rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all access for development
    // Security is handled by Clerk authentication at the application level
    match /{document=**} {
      allow read, write: if true;
    }
  }
}