rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow read/write access to all files for now
    // This is for development - you should restrict this in production
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
